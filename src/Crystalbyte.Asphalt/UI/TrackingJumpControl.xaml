<UserControl x:Class="Crystalbyte.Asphalt.UI.TrackingJumpControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:contexts="clr-namespace:Crystalbyte.Asphalt.Contexts"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             FontFamily="{StaticResource PhoneFontFamilyNormal}" FontSize="{StaticResource PhoneFontSizeNormal}"
             Foreground="{StaticResource PhoneForegroundBrush}" d:DataContext="{d:DesignInstance Type=contexts:LocationTracker}"
             d:DesignHeight="480" d:DesignWidth="480" mc:Ignorable="d">
    <Grid x:Name="LayoutRoot" Background="Green">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Image Grid.Column="0" Width="32" Height="32" Source="/Assets/ApplicationIcon.png" />
        <TextBlock Grid.Column="1" Margin="12" Text="Tap to return to tracking page ..." />
        <Rectangle Grid.Column="0" Grid.ColumnSpan="2" Fill="Transparent" Tap="OnOverlayRectangleTapped" />
    </Grid>
</UserControl>